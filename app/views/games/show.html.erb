<%= @board.word %>

<%= form_for :guess, url: game_guesses_path(@board.game_id) do |form| %>
  <% @board.letters.each do |letter| %>
    <%= form.submit letter, name: "guess[letter]", disabled: @board.guessed?(letter) %>
  <% end %>
<% end %>
